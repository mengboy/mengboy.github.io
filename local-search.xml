<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>websocket引入epoll机制</title>
    <link href="/2020/09/15/websocket-epoll/"/>
    <url>/2020/09/15/websocket-epoll/</url>
    
    <content type="html"><![CDATA[<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>正常情况下，不使用epoll，每建立一个ws连接我们都会启一个goroutine来处理客户端发来的消息，如果客户端没有消息，这个goroutine就会阻塞在读消息的状态。有多少个连接就会有多少个goroutine，虽然goroutine比较轻量，但在大量连接的情况下还是比较耗费资源还是很大的。<br>使用epoll后会把所有ws连接的fd(文件描述符)放入一个epoll队列中，只启一个goroutine对epoll wait进行轮询获取缓冲区有内容的ws连接。这样就把之前要起n个goroutine变成了1个，随着连接数的增加性能的提升也会越来越明显。<br><img src="/images/websocket-epoll/websocket-epoll.png"></p><h2 id="实践"><a href="#实践" class="headerlink" title="实践"></a>实践</h2><p>具体<a href="https://github.com/mengboy/1m-go-websockets">github demo源码</a> ，参考<a href="https://github.com/eranyanay/1m-go-websockets">1m-go-websockets</a> ，原仓库只有linux下的实现，因为平时主要使用mac开发所以参考Linux的实现增加了mac下的实现。<br>demo中用到了两种websocket框架，分别是<a href="github.com/gorilla/websocket">gorilla/websocket</a> 和 <a href="github.com/gobwas/ws">gobwas/ws</a>。实际应用中发现了一个问题，因为平时主要使用gorilla/websocket，并且它的start数也远超gobwas/ws，所以在引入epoll时首选还是gorilla/websocket，然后开发调试过程中客户端一开始并发发msg给服务端，服务端就会有消息漏掉没处理的情况。<br>举个例子，客户端多个消息并发通知到服务端，客户端发第三次消息了，服务端才可能处理第二次消息。很明显并不是每次消息都触发了epoll的回调事件，这里epoll使用的水平触发，根据水平触发的定义：当被监控的文件描述符上有可读写事件发生时，就会一直触发epoll回调，这里没有触发说明文件描述上没有可读事件，基本上可以猜测epoll还没来的及通知时，fd对应的内容已经被读取完毕了。<br>理想中的触发流程：<br><img src="/images/websocket-epoll/expectflow.png"><br>实际触发流程:<br><img src="/images/websocket-epoll/realflow.png"><br>看到这里可能会有同学想问，既然msg2没有处理，那我们主动处理，不等epoll触发可不可以呢？我们看下gorilla/websocket读取消息的源码  </p><pre><code class="hljs stata"><span class="hljs-keyword">for</span> &#123;connections, <span class="hljs-keyword">err</span> := epoller.Wait()<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> != nil &#123;<span class="hljs-keyword">log</span>.Printf(<span class="hljs-string">&quot;Failed to epoll wait %v&quot;</span>, <span class="hljs-keyword">err</span>)<span class="hljs-keyword">continue</span>&#125;time.<span class="hljs-keyword">Sleep</span>(5)<span class="hljs-keyword">for</span> _, conn := <span class="hljs-keyword">range</span> connections &#123;<span class="hljs-keyword">if</span> conn == nil &#123;<span class="hljs-keyword">break</span>&#125;_, msg, <span class="hljs-keyword">err</span> := conn.ReadMessage()<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> != nil &#123;<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> := epoller.Remove(conn); <span class="hljs-keyword">err</span> != nil &#123;<span class="hljs-keyword">log</span>.Printf(<span class="hljs-string">&quot;Failed to remove %v&quot;</span>, <span class="hljs-keyword">err</span>)&#125;conn.<span class="hljs-keyword">Close</span>()&#125; <span class="hljs-keyword">else</span> &#123;<span class="hljs-keyword">log</span>.Printf(<span class="hljs-string">&quot;msg: %s&quot;</span>, <span class="hljs-built_in">string</span>(msg))&#125;&#125;&#125;&#125;</code></pre><p>从epoll wait拿到可读连接，到底下的读是一一对应的，我们如果想把conn 缓冲区处理完怎么办，只能接着循环 <em>conn.ReadMessage()</em> 但是这个函数并不会返回缓冲区是否处理完毕，当缓冲区和fd都没有东西读时它就被阻塞了，就是下面代码TODO部分无法实现，如果能够实现，epoll 就可以使用边沿触发的模式，理论上相比水平触发更高效。</p><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">Start</span><span class="hljs-params">()</span></span> &#123;<span class="hljs-keyword">for</span> &#123;connections, err := epoller.Wait()<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;log.Printf(<span class="hljs-string">&quot;Failed to epoll wait %v&quot;</span>, err)<span class="hljs-keyword">continue</span>&#125;time.Sleep(<span class="hljs-number">5</span>)        wg := &amp;sync.WatiGroup&#123;&#125;<span class="hljs-keyword">for</span> _, conn := <span class="hljs-keyword">range</span> connections &#123;<span class="hljs-keyword">if</span> conn == <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">break</span>&#125;            wg.Add(<span class="hljs-number">1</span>)<span class="hljs-keyword">go</span> <span class="hljs-function"><span class="hljs-keyword">func</span><span class="hljs-params">()</span></span> &#123;                <span class="hljs-keyword">defer</span> wg.done<span class="hljs-keyword">for</span> &#123;_, msg, err := conn.ReadMessage()<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">if</span> err := epoller.Remove(conn); err != <span class="hljs-literal">nil</span> &#123;log.Printf(<span class="hljs-string">&quot;Failed to remove %v&quot;</span>, err)&#125;conn.Close()&#125; <span class="hljs-keyword">else</span> &#123;log.Printf(<span class="hljs-string">&quot;msg: %s&quot;</span>, <span class="hljs-keyword">string</span>(msg))&#125;<span class="hljs-comment">// TODO 没有内容可读时退出，等待下一次epoll触发</span>&#125;&#125;()&#125;        wg.Wait()&#125;&#125;</code></pre><h2 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h2><p>为什么gorilla/websocket在这种场景下使用会有这种问题，而gobwas/ws没有？下面我们通过分析两者的读实现找下原因。</p><h3 id="gorilla-websocket-读实现"><a href="#gorilla-websocket-读实现" class="headerlink" title="gorilla/websocket 读实现"></a>gorilla/websocket 读实现</h3><p>从 <em>conn.ReadMessage()</em> 点进去,可以看到这函数调了c.NextReader</p><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(c *Conn)</span> <span class="hljs-title">ReadMessage</span><span class="hljs-params">()</span> <span class="hljs-params">(messageType <span class="hljs-keyword">int</span>, p []<span class="hljs-keyword">byte</span>, err error)</span></span> &#123;<span class="hljs-keyword">var</span> r io.ReadermessageType, r, err = c.NextReader()<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> messageType, <span class="hljs-literal">nil</span>, err&#125;p, err = ioutil.ReadAll(r)<span class="hljs-keyword">return</span> messageType, p, err&#125;</code></pre><p>再点进c.NextReader可以看到实际返回要读的对象是messageReader  </p><pre><code class="hljs swift"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(<span class="hljs-built_in">c</span> *Conn)</span></span> <span class="hljs-type">NextReader</span>() (messageType int, r io.<span class="hljs-type">Reader</span>, err error) &#123;<span class="hljs-keyword">for</span> <span class="hljs-built_in">c</span>.readErr == <span class="hljs-literal">nil</span> &#123;frameType, err := <span class="hljs-built_in">c</span>.advanceFrame()<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-built_in">c</span>.readErr = hideTempErr(err)<span class="hljs-keyword">break</span>&#125;<span class="hljs-keyword">if</span> frameType == <span class="hljs-type">TextMessage</span> || frameType == <span class="hljs-type">BinaryMessage</span> &#123;<span class="hljs-built_in">c</span>.messageReader = &amp;messageReader&#123;<span class="hljs-built_in">c</span>&#125;<span class="hljs-built_in">c</span>.reader = <span class="hljs-built_in">c</span>.messageReader<span class="hljs-keyword">if</span> <span class="hljs-built_in">c</span>.readDecompress &#123;<span class="hljs-built_in">c</span>.reader = <span class="hljs-built_in">c</span>.newDecompressionReader(<span class="hljs-built_in">c</span>.reader)&#125;            <span class="hljs-comment">// 返回messageReader</span><span class="hljs-keyword">return</span> frameType, <span class="hljs-built_in">c</span>.reader, <span class="hljs-literal">nil</span>&#125;&#125;&#125;</code></pre><p>接着看messageReader的实现, 通过看messageReader实现的Read方法发现，实际读取的内容来自c.br   </p><pre><code class="hljs swift"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-params">(r *messageReader)</span></span> <span class="hljs-type">Read</span>(b []byte) (int, error) &#123;<span class="hljs-keyword">for</span> <span class="hljs-built_in">c</span>.readErr == <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">if</span> <span class="hljs-built_in">c</span>.readRemaining &gt; <span class="hljs-number">0</span> &#123;<span class="hljs-keyword">if</span> int64(len(b)) &gt; <span class="hljs-built_in">c</span>.readRemaining &#123;b = b[:<span class="hljs-built_in">c</span>.readRemaining]&#125;            <span class="hljs-comment">// 实际从c.br读取的内容</span>n, err := <span class="hljs-built_in">c</span>.br.<span class="hljs-type">Read</span>(b)<span class="hljs-built_in">c</span>.readErr = hideTempErr(err)<span class="hljs-keyword">if</span> <span class="hljs-built_in">c</span>.isServer &#123;<span class="hljs-built_in">c</span>.readMaskPos = maskBytes(<span class="hljs-built_in">c</span>.readMaskKey, <span class="hljs-built_in">c</span>.readMaskPos, b[:n])&#125;rem := <span class="hljs-built_in">c</span>.readRemainingrem -= int64(n)<span class="hljs-built_in">c</span>.setReadRemaining(rem)<span class="hljs-keyword">if</span> <span class="hljs-built_in">c</span>.readRemaining &gt; <span class="hljs-number">0</span> &amp;&amp; <span class="hljs-built_in">c</span>.readErr == io.<span class="hljs-type">EOF</span> &#123;<span class="hljs-built_in">c</span>.readErr = errUnexpectedEOF&#125;<span class="hljs-keyword">return</span> n, <span class="hljs-built_in">c</span>.readErr&#125;&#125;</code></pre><p>c.br是go自身提供的缓冲读io的对象，看到这里已经很明朗了，就是这里在读的时候，把fd里要读的内容读到了本地缓冲区，导致上图msg2的时候epoll没有再次触发。</p><pre><code class="hljs gauss"><span class="hljs-built_in">type</span> Conn <span class="hljs-keyword">struct</span> &#123;conn        net.Conn<span class="hljs-comment">// Read fields</span>reader  io.ReadCloser <span class="hljs-comment">// the current reader returned to the application</span>readErr <span class="hljs-built_in">error</span>br      *bufio.Reader&#125;</code></pre><p>上面还有一个用到比较多的函数 c.advanceFrame()，这个函数是用来处理每次要读多少字节的，因为websocket消息是一帧一帧的，读的时候也不能读多或者读少。</p><h3 id="gobwas-ws读实现"><a href="#gobwas-ws读实现" class="headerlink" title="gobwas/ws读实现"></a>gobwas/ws读实现</h3><p>gobwas/ws 是用的utils包里的ReadClientData方法读取client发的消息的，最终是调的readData方法  </p><pre><code class="hljs reasonml">func <span class="hljs-constructor">ReadClientData(<span class="hljs-params">rw</span> <span class="hljs-params">io</span>.ReadWriter)</span> (<span class="hljs-literal">[]</span>byte, ws.OpCode, error) &#123;return <span class="hljs-constructor">ReadData(<span class="hljs-params">rw</span>, <span class="hljs-params">ws</span>.StateServerSide)</span>&#125;func <span class="hljs-constructor">ReadData(<span class="hljs-params">rw</span> <span class="hljs-params">io</span>.ReadWriter, <span class="hljs-params">s</span> <span class="hljs-params">ws</span>.State)</span> (<span class="hljs-literal">[]</span>byte, ws.OpCode, error) &#123;return read<span class="hljs-constructor">Data(<span class="hljs-params">rw</span>, <span class="hljs-params">s</span>, <span class="hljs-params">ws</span>.OpText|<span class="hljs-params">ws</span>.OpBinary)</span>&#125;</code></pre><p>readData具体实现,这里根据net.Conn连接创建了一个Reader</p><pre><code class="hljs go"><span class="hljs-function"><span class="hljs-keyword">func</span> <span class="hljs-title">readData</span><span class="hljs-params">(rw io.ReadWriter, s ws.State, want ws.OpCode)</span> <span class="hljs-params">([]<span class="hljs-keyword">byte</span>, ws.OpCode, error)</span></span> &#123;controlHandler := ControlFrameHandler(rw, s)    <span class="hljs-comment">// rw 就是 net.Conn</span>rd := Reader&#123;Source:          rw,State:           s,CheckUTF8:       <span class="hljs-literal">true</span>,SkipHeaderCheck: <span class="hljs-literal">false</span>,OnIntermediate:  controlHandler,&#125;<span class="hljs-keyword">for</span> &#123;hdr, err := rd.NextFrame()<span class="hljs-keyword">if</span> err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>, <span class="hljs-number">0</span>, err&#125;<span class="hljs-keyword">if</span> hdr.OpCode.IsControl() &#123;<span class="hljs-keyword">if</span> err := controlHandler(hdr, &amp;rd); err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>, <span class="hljs-number">0</span>, err&#125;<span class="hljs-keyword">continue</span>&#125;<span class="hljs-keyword">if</span> hdr.OpCode&amp;want == <span class="hljs-number">0</span> &#123;<span class="hljs-keyword">if</span> err := rd.Discard(); err != <span class="hljs-literal">nil</span> &#123;<span class="hljs-keyword">return</span> <span class="hljs-literal">nil</span>, <span class="hljs-number">0</span>, err&#125;<span class="hljs-keyword">continue</span>&#125;bts, err := ioutil.ReadAll(&amp;rd)<span class="hljs-keyword">return</span> bts, hdr.OpCode, err&#125;&#125;</code></pre><p>Reader 结构， 可以看到这个是gobwas/ws自己实现的一个无buf Reader</p><pre><code class="hljs elm"><span class="hljs-keyword">type</span> <span class="hljs-type">Reader</span> struct &#123;<span class="hljs-type">Source</span> io.<span class="hljs-type">Reader</span><span class="hljs-type">State</span>  ws.<span class="hljs-type">State</span>// <span class="hljs-type">SkipHeaderCheck</span> disables checking header bits to be <span class="hljs-type">RFC6455</span> compliant.<span class="hljs-type">SkipHeaderCheck</span> bool// <span class="hljs-type">CheckUTF8</span> enables <span class="hljs-type">UTF</span>-8 checks for text frames payload. <span class="hljs-type">If</span> incoming// bytes are not valid <span class="hljs-type">UTF</span>-8 sequence, <span class="hljs-type">ErrInvalidUTF8</span> returned.<span class="hljs-type">CheckUTF8</span> bool// <span class="hljs-type">TODO(gobwas)</span>: add max frame size limit here.<span class="hljs-type">OnContinuation</span> <span class="hljs-type">FrameHandlerFunc</span><span class="hljs-type">OnIntermediate</span> <span class="hljs-type">FrameHandlerFunc</span>opCode ws.<span class="hljs-type">OpCode</span>        // <span class="hljs-type">Used</span> to store message op code on fragmentation.frame  io.<span class="hljs-type">Reader</span>        // <span class="hljs-type">Used</span> to as frame reader.raw    io.<span class="hljs-type">LimitedReader</span> // <span class="hljs-type">Used</span> to discard frames without cipher.utf8   <span class="hljs-type">UTF8Reader</span>       // <span class="hljs-type">Used</span> to check <span class="hljs-type">UTF8</span> sequences if <span class="hljs-type">CheckUTF8</span> is true.&#125;</code></pre><p>具体怎么读，读多少字节是在rd.NextFrame()方法中实现的，这里可以看到gobwas/ws每次都是直接读net.Conn也就是fd，并且每次当前帧的内容，所以刚好对应上图理想中的触发流程。  </p><pre><code class="hljs stata">func (r *Reader) NextFrame() (hdr ws.Header, <span class="hljs-keyword">err</span> <span class="hljs-keyword">error</span>) &#123;    <span class="hljs-comment">// 解析当前帧报文</span>hdr, <span class="hljs-keyword">err</span> = ws.ReadHeader(r.Source)<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> == io.EOF &amp;&amp; r.fragmented() &#123;<span class="hljs-keyword">err</span> = io.ErrUnexpectedEOF&#125;<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> == nil &amp;&amp; !r.SkipHeaderCheck &#123;<span class="hljs-keyword">err</span> = ws.CheckHeader(hdr, r.State)&#125;<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> != nil &#123;<span class="hljs-keyword">return</span> hdr, <span class="hljs-keyword">err</span>&#125;<span class="hljs-comment">// 设置读取长度， 这里r.Source 就是net.Conn</span>r.raw = io.LimitedReader&#123;r.Source, hdr.Length&#125;frame := io.Reader(&amp;r.raw)<span class="hljs-keyword">if</span> hdr.Masked &#123;frame = NewCipherReader(frame, hdr.Mask)&#125;<span class="hljs-keyword">if</span> r.fragmented() &#123;<span class="hljs-keyword">if</span> hdr.OpCode.IsControl() &#123;<span class="hljs-keyword">if</span> cb := r.OnIntermediate; cb != nil &#123;<span class="hljs-keyword">err</span> = cb(hdr, frame)&#125;<span class="hljs-keyword">if</span> <span class="hljs-keyword">err</span> == nil &#123;<span class="hljs-comment">// Ensure that src is empty.</span>                <span class="hljs-comment">// 直接从net.Conn读取hdr.Length长度的内容</span>_, <span class="hljs-keyword">err</span> = io.<span class="hljs-keyword">Copy</span>(ioutil.<span class="hljs-keyword">Discard</span>, &amp;r.raw)&#125;<span class="hljs-keyword">return</span>&#125;&#125; <span class="hljs-keyword">else</span> &#123;r.opCode = hdr.OpCode&#125;<span class="hljs-keyword">if</span> r.CheckUTF8 &amp;&amp; (hdr.OpCode == ws.OpText || (r.fragmented() &amp;&amp; r.opCode == ws.OpText)) &#123;r.utf8.Source = frameframe = &amp;r.utf8&#125;<span class="hljs-comment">// Save reader with ciphering and other streaming checks.</span>r.frame = frame<span class="hljs-keyword">if</span> hdr.OpCode == ws.OpContinuation &#123;<span class="hljs-keyword">if</span> cb := r.OnContinuation; cb != nil &#123;<span class="hljs-keyword">err</span> = cb(hdr, frame)&#125;&#125;<span class="hljs-keyword">if</span> hdr.Fin &#123;r.State = r.State.<span class="hljs-keyword">Clear</span>(ws.StateFragmented)&#125; <span class="hljs-keyword">else</span> &#123;r.State = r.State.<span class="hljs-keyword">Set</span>(ws.StateFragmented)&#125;<span class="hljs-keyword">return</span>&#125;</code></pre><h3 id="两种方式的读比较"><a href="#两种方式的读比较" class="headerlink" title="两种方式的读比较"></a>两种方式的读比较</h3><p>gorilla/websocket加了buf读的方式理论上比gobwas/ws性能更高，如果能提供个buf和fd已读完读返回就好了，这样就可以直接使用epoll边沿触发模式。如果实在想用gorilla/websocket，这里也有一个解决方案，就是客户端加一个ping的控制帧消息，gorilla/websocket在接收到ping/pong之类的控制帧消息时，不会把这些消息暴漏给用户处理，而是在内部自己处理，处理完之后会接着处理剩下的消息，直到处理的消息不是ping/pong为止，这样就可以使用ping控制型帧强制触发epoll事件，保证业务消息能够及时处理。</p><h2 id="参考以及源码"><a href="#参考以及源码" class="headerlink" title="参考以及源码"></a>参考以及源码</h2><ul><li><a href="https://github.com/mengboy/1m-go-websockets">1m-go-websockets</a></li><li><a href="https://colobu.com/2019/02/23/1m-go-tcp-connection/">百万 Go TCP 连接的思考: epoll方式减少资源占用</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>网络协议</category>
      
      <category>go</category>
      
    </categories>
    
    
    <tags>
      
      <tag>websocket</tag>
      
      <tag>epoll</tag>
      
      <tag>gorilla/websocket</tag>
      
      <tag>gobwas/ws</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>sse(serve send events) 服务端推送技术</title>
    <link href="/2020/09/12/sse/"/>
    <url>/2020/09/12/sse/</url>
    
    <content type="html"><![CDATA[<h3 id="什么是sse"><a href="#什么是sse" class="headerlink" title="什么是sse"></a>什么是sse</h3><p>sse 指 serve send events，是一种基于http的服务端想客户端推送事件的技术，通常http请求都是请求–&gt;应答的模式，一次request–&gt;response结束后，连接就断开了，前端需要请求新的资源就需要重新建立连接，再次发起请求，当然http2.0后可以使用多路复用，不必建立新的连接。而sse指连接建立成功后，后端可以不断向前端推送消息。<br><img src="/images/sse/httpvssse.png"><br>sse 连接的header Content-Type: text/event-stream，后端响应可以在EventStream选项卡中看到<br><img src="/images/sse/httpsseheader.png"><br><img src="/images/sse/sseeventstream.png"><br>其他具体的api介绍，比如web端是通过什么接口处理后端推送，以及后端推送的数据格式可以参考<a href="https://developer.mozilla.org/zh-CN/docs/Server-sent_events/Using_server-sent_events#%E4%BA%8B%E4%BB%B6%E6%B5%81%E6%A0%BC%E5%BC%8F">MDN文档</a></p><h3 id="http2-0-前后的sse"><a href="#http2-0-前后的sse" class="headerlink" title="http2.0 前后的sse"></a>http2.0 前后的sse</h3><p>http2一个最大的特性是引入流的多路复用机制，sse在http2.0前后的一个差异也主要是因为多路用。</p><h4 id="http1-x的sse"><a href="#http1-x的sse" class="headerlink" title="http1.x的sse"></a>http1.x的sse</h4><p>http1.x没有多路复用，在已建立sse的情况下，如果前端想要发消息给后端就需要建立新的连接。这对于需要前端频繁向后端请求资源的场景是一个巨大的消耗。<br><img src="/images/sse/http1ssedemo.png"><br>图中示例c1、c2是来自两个sse连接的推送，chrome 调试里可以看到协议是http1.1以及connection id 分别是9003、9307，这就表示http1 后端推送和前端请求没法在同一个连接里进行。http1下还有一个限制是同一域名sse连接数和浏览器最大http并发连接数一样，一般是6个，可以使用demo打开3个tab建立6个连接后再打开一个具体观察，而http2.0这个数默认是100。MDN文档里也有对这个的说明。</p><h4 id="http2的sse"><a href="#http2的sse" class="headerlink" title="http2的sse"></a>http2的sse</h4><p><img src="/images/sse/http2sse.png"><br>由于http2的多路复用，可以明显的看到这里两个sse的连接的connection ID都是15522，说明两个sse用的是同一个http连接。也就是说前端向后端请求和后端向前端推送可以在同一个连接里完成，有接触过websocket的同学会发现http2的sse基本上可以取代websocket。websocket在同链路上的全双工通信，http2的sse也可以实现。</p><h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h3><p>聊天室的消息推送，直播过程中的消息推送活动推送，某些场景下的状态更改等。任何需要高频http轮询或者websocket的场景都可以使用sse替代。使用sse替代的一些优势，如果是替代http轮询很明显不需要前端再发请求了，会节省下处理前端请求的资源；如果是替代websocket，http1的sse还不能完全替代websocket，但是http2的完全可以，http2的多路复用完美的解决了建立新连接带来的巨大性能消耗，基本上两者都是同一连接上的全双工通信。</p><h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul><li><a href="https://github.com/mengboy/sse-demo">http1 sse demo</a></li><li><a href="https://github.com/kaitmore/sse-envoy">http2 sse demo</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Server-sent_events/Using_server-sent_events#%E4%BA%8B%E4%BB%B6%E6%B5%81%E6%A0%BC%E5%BC%8F">sse MDN</a></li></ul>]]></content>
    
    
    <categories>
      
      <category>网络协议</category>
      
    </categories>
    
    
    <tags>
      
      <tag>http1.x</tag>
      
      <tag>http2.0</tag>
      
      <tag>sse</tag>
      
      <tag>go</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
